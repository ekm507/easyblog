# The base URL of the site
base_url = "https://example.com"

# the website title
title = "وبلاگ من"

# The site description, will be used in feeds
description = "توضیحات وبلاگ خود را در اینجا بنویسید"

# When set to "true", a feed is automatically generated.
generate_feed = true
# the feed url will be : base_url/rss.xml
